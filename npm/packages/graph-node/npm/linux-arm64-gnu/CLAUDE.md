# @ruvector/graph-node-linux-arm64-gnu

Per-platform NAPI sub-package: Linux arm64 (glibc) binary for `@ruvector/graph-node`.

- `package.json` — `os: ["linux"]`, `cpu: ["arm64"]`, libc glibc, main `graph-node.linux-arm64-gnu.node`.
