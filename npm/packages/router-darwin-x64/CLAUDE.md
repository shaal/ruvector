# @ruvector/router-darwin-x64

Per-platform NAPI sub-package: macOS x64 native binary for `@ruvector/router`.

- `package.json` — v0.1.30; `os: ["darwin"]`, `cpu: ["x64"]`, main `ruvector-router.darwin-x64.node`.

The `.node` binary is produced by the parent `@ruvector/router` build and dropped here at publish time.
