# src/schemas/

Zod schema definitions for generator inputs/outputs and graph types. Published as the `./schemas` subpath export.

- `index.ts` — barrel of all schemas used by generators in `../generators/`.
