# native/

Native C++ source for the `@ruvector/spiking-neural` add-on. Built by `node-gyp` via `binding.gyp` in the parent directory.

- `snn_simd.cpp` — SIMD-accelerated SNN core (LIF neurons, STDP learning).
