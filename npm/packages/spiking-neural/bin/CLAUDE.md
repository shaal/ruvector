# bin/

Published CLI for `@ruvector/spiking-neural`.

- `cli.js` — mapped to both `spiking-neural` and `snn` bins. Subcommands include `test`, `benchmark`, and `demo pattern` (invoked by the package's `test` / `benchmark` / `demo` npm scripts).
