# ruvbot / tests / unit / workers

Unit tests for the background worker abstraction.

## Files
- `background-workers.test.ts` - Verifies job scheduling, retry, and
  concurrency limits of `src/infrastructure/workers`.
