# ruvbot / tests / unit / api

Unit tests for ruvbot's HTTP API handlers.

## Files
- `endpoints.test.ts` - Verifies routing, request validation, and
  response shape of the endpoints exposed by `src/server.ts` /
  `src/api`.
