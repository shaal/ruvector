# ruvbot / tests / mocks

Mock implementations of external collaborators used in unit and
integration tests.

## Files
- `postgres.mock.ts` - In-memory stand-in for the Postgres client.
- `slack.mock.ts` - Mock Slack bolt / web-api surface.
- `index.ts` - Barrel exporting all mocks.
