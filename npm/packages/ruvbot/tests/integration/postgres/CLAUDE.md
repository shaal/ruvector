# ruvbot / tests / integration / postgres

Tests for the Postgres persistence adapter (ADR-003).

## Files
- `persistence.test.ts` - CRUD + transaction behavior of the Postgres
  repositories defined in `src/infrastructure/persistence/`.
