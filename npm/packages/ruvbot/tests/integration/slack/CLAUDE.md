# ruvbot / tests / integration / slack

Tests for the Slack channel integration.

## Files
- `integration.test.ts` - Exercises the `SlackAdapter` and the Slack
  bolt entry points against the mocks in `tests/mocks/slack.mock.ts`.
