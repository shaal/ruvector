# ruvbot / tests / integration / multitenancy

Multi-tenancy isolation tests (ADR-002).

## Files
- `isolation.test.ts` - Verifies tenant/workspace boundaries are
  enforced across persistence, sessions, and memory.
