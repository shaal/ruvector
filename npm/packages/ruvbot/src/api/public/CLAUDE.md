# ruvbot / src / api / public

Static assets served from ruvbot's HTTP API.

## Files
- `index.html` - Browser chat UI for interacting with a running ruvbot
  instance (ADR-015). Bundled via `package.json` `files` so it ships
  with the published npm tarball.
