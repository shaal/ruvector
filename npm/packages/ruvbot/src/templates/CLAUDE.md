# ruvbot / src / templates

Conversation / agent templates used by the `init` CLI command and by
the API to bootstrap new agents from preset personas.

## Files
- `index.ts` - Barrel exporting template definitions.
