# ruvbot / src / core / skill

Skill domain contracts: defines what a "skill" is (name, schema,
handler) and how the framework registers / discovers them. Concrete
implementations live in `../../skills/`.

## Files
- `index.ts` - Barrel exporting Skill interfaces and helpers.
