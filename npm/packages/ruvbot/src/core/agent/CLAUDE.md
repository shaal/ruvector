# ruvbot / src / core / agent

Agent aggregate. Defines the `Agent` domain object, lifecycle, and
behaviors that the framework instantiates per `AgentConfig`.

## Files
- `index.ts` - Barrel exposing the Agent factory/class and related
  helpers. Consumed by `core/index.ts`.
