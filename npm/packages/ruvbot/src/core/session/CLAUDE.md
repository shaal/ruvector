# ruvbot / src / core / session

Session lifecycle management: create/resume/expire sessions, append
turns, and dispatch session events.

## Files
- `index.ts` - Barrel exposing the session manager / repository
  contracts re-exported from `core/index.ts`.
