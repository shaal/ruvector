# @ruvector/router-linux-x64-gnu

Platform-specific native (N-API) binary for `@ruvector/router` — Linux x64 glibc. Pulled in as an optional dependency by the main router package.

## Files

- `package.json` — `@ruvector/router-linux-x64-gnu` v0.1.30. `os: ["linux"]`, `cpu: ["x64"]`, `libc: ["glibc"]`. Main and only published file: `ruvector-router.linux-x64-gnu.node`.

## Related

- Parent: `@ruvector/router` (cross-platform JS loader).
- Sibling: `npm/packages/router-darwin-arm64` and other `router-*` directories.
