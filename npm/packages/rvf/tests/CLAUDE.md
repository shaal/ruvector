# rvf/tests

Test files for `@ruvector/rvf`. Driven by Jest (`npm test`).

## Files

- `test-id-mapping.js` — small standalone test for ID-mapping behavior between the SDK and the underlying backend.
