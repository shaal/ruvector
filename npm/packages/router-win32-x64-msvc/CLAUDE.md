# @ruvector/router-win32-x64-msvc

Per-platform NAPI sub-package: Windows x64 (MSVC) native binary for `@ruvector/router`.

- `package.json` — v0.1.30; `os: ["win32"]`, `cpu: ["x64"]`, main `ruvector-router.win32-x64-msvc.node`.

The `.node` binary is produced by the parent `@ruvector/router` build and dropped here at publish time.
