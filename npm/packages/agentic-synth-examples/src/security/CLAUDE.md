# src/security/

Synthetic security/red-team data generator.

- `index.ts` — exports `SecurityTestingGenerator` and types: `VulnerabilityTestCase`, `SecurityLogEntry`, `AnomalyPattern`, `PenetrationTestScenario`, `VulnerabilitySeverity`, `VulnerabilityType`.
