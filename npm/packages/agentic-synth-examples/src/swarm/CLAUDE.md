# src/swarm/

Multi-agent swarm coordination example.

- `index.ts` — exports `SwarmCoordinator` and types `Agent`, `AgentMemory`, `CoordinationTask`, `DistributedLearningPattern`, `SwarmStatistics`, `AgentRole`, `CoordinationStrategy`.
