# src/stock-market/

Stock market simulator facade.

- `index.ts` — exports `StockMarketSimulator` and types `StockMarketConfig`, `OHLCVData`, `MarketNewsEvent`, `MarketCondition`, `MarketStatistics`.
