# src/generators/

Two standalone generator implementations re-exported from the package root.

- `self-learning.ts` — adaptive `SelfLearningGenerator` with feedback metrics.
- `stock-market.ts` — OHLCV and market-event simulator (delegated to by `src/stock-market/`).
