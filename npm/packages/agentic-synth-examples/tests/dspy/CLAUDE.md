# tests/dspy/

Vitest tests for the DSPy training/benchmark module (`src/dspy/`).

- `benchmark.test.ts` — tests for `MultiModelBenchmark` and metric collection.
- `training-session.test.ts` — tests for `DSPyTrainingSession` and per-provider agents.
