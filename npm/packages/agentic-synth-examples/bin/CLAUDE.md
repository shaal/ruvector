# bin/

CLI launcher for `@ruvector/agentic-synth-examples`.

- `cli.js` — executable; mapped to the `agentic-synth-examples` bin in parent `package.json`. Routes subcommands into the example generators in `../src/`.
