# @ruvector/sona-linux-x64-musl

Native N-API binary for `@ruvector/sona` on Linux x64 (musl, e.g. Alpine).

## Files

- `package.json` — `@ruvector/sona-linux-x64-musl`. `os: ["linux"]`, `cpu: ["x64"]`, `libc: ["musl"]`. Main and only published file: `sona.linux-x64-musl.node`.

The parent `@ruvector/sona` lists this as an `optionalDependency`.
