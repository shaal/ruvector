# @ruvector/sona-linux-arm64-gnu

Native N-API binary for `@ruvector/sona` on Linux ARM64 (glibc).

## Files

- `package.json` — `@ruvector/sona-linux-arm64-gnu`. `os: ["linux"]`, `cpu: ["arm64"]`, `libc: ["glibc"]`. Main and only published file: `sona.linux-arm64-gnu.node`.

The parent `@ruvector/sona` lists this as an `optionalDependency`.
