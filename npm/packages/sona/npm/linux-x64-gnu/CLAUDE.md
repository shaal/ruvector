# @ruvector/sona-linux-x64-gnu

Native N-API binary for `@ruvector/sona` on Linux x64 (glibc).

## Files

- `package.json` — `@ruvector/sona-linux-x64-gnu`. `os: ["linux"]`, `cpu: ["x64"]`, `libc: ["glibc"]`. Main and only published file: `sona.linux-x64-gnu.node`.

The parent `@ruvector/sona` lists this as an `optionalDependency`.
