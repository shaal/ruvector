# @ruvector/sona-win32-x64-msvc

Native N-API binary for `@ruvector/sona` on Windows x64 (MSVC).

## Files

- `package.json` — `@ruvector/sona-win32-x64-msvc`. `os: ["win32"]`, `cpu: ["x64"]`. Main and only published file: `sona.win32-x64-msvc.node`.

The parent `@ruvector/sona` lists this as an `optionalDependency`.
