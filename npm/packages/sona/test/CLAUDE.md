# sona/test

Node-test (`node:test`) smoke tests for `@ruvector/sona`. Run via `npm test`.

## Files

- `basic.test.js` — verifies `SonaEngine` creation (`new SonaEngine(128)`), `isEnabled()`, and `SonaEngine.withConfig({hiddenDim, microLoraRank, baseLoraRank})`.
