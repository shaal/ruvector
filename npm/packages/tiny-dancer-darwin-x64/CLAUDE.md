# @ruvector/tiny-dancer-darwin-x64

Per-platform NAPI sub-package: macOS x64 native binary for `@ruvector/tiny-dancer`.

- `package.json` — v0.1.15; `os: ["darwin"]`, `cpu: ["x64"]`, main `ruvector-tiny-dancer.darwin-x64.node`.

The `.node` binary is produced by the parent `@ruvector/tiny-dancer` build and dropped here at publish time.
