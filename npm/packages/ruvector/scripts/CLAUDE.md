# scripts/

Build/publish helper scripts for the `ruvector` package.

- `verify-dist.js` — invoked by `npm run verify-dist` (and `prepack`/`prepublishOnly`); validates that `dist/` is complete and correct before publishing.
