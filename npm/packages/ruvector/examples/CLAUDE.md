# examples/

Usage examples for the `ruvector` package.

- `api-usage.js` — programmatic API walkthrough (VectorDb insert/search/etc).
- `cli-demo.sh` — shell script demonstrating the `ruvector` CLI commands.
