# src/optimizer/

Settings / context optimizer for `ruvector`'s hooks system (Claude Code integration). Pure JS modules.

- `context.js` — context gathering for the optimizer.
- `settings-generator.js` — produces optimized hook/settings configurations.
- `tool-schemas.js` — JSON schemas for tool inputs/outputs used by the optimizer.
