# src/benchmarks/

Benchmark sources for `@ruvector/ruvllm` (TS + compiled JS/d.ts).

- `index.ts` — barrel.
- `embedding-benchmark.ts` — embedding throughput / quality benchmarks.
- `model-comparison.ts` — side-by-side multi-model comparison harness.
- `routing-benchmark.ts` — router decision-quality / latency benchmark.
