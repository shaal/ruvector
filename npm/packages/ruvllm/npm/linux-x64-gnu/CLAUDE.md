# @ruvector/ruvllm-linux-x64-gnu

Per-platform NAPI sub-package: Linux x64 (glibc) binary for `@ruvector/ruvllm`.

- `package.json` — `os: ["linux"]`, `cpu: ["x64"]`, libc glibc, main `ruvllm.linux-x64-gnu.node`.
