# agentic-synth/src/types

Additional shared type re-exports for `@ruvector/agentic-synth`.

## Files

- `index.ts` — barrel re-exports of types declared in `../types.ts` (the main schema file), grouped for subpath imports.
