# agentic-synth/src/api

HTTP API client used by `@ruvector/agentic-synth` generators.

## Files

- `client.js` — low-level HTTP client wrapping provider calls (Gemini / OpenRouter) with retries and timeouts driven by `SynthConfig`.
