# agentic-synth/src/config

Config loading for `@ruvector/agentic-synth`.

## Files

- `config.js` — loads `synth.config.json` (or env vars / `.env`), merges with `SynthConfigSchema` defaults, and returns a validated `SynthConfig`.
