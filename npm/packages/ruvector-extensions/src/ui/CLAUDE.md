# ruvector-extensions/src/ui

Static UI assets served by `src/ui-server.ts`.

## Files

- `index.html` — UI shell (graph visualization).
- `app.js` — client-side JS (connects to the Express + WebSocket server).
- `styles.css` — page styling.
