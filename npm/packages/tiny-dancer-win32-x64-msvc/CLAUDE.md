# @ruvector/tiny-dancer-win32-x64-msvc

Per-platform NAPI sub-package: Windows x64 (MSVC) native binary for `@ruvector/tiny-dancer`.

- `package.json` — v0.1.15; `os: ["win32"]`, `cpu: ["x64"]`, main `ruvector-tiny-dancer.win32-x64-msvc.node`.

The `.node` binary is produced by the parent `@ruvector/tiny-dancer` build and dropped here at publish time.
