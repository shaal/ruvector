# ruvector-cli/scripts

Helper scripts shipped alongside the CLI.

- `statusline-command.sh` — POSIX shell statusline integration for Claude Code.
- `statusline-command.ps1` — PowerShell equivalent for Windows.
