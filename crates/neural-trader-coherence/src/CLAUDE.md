# neural-trader-coherence/src

- `lib.rs` — sole source file. Declares the coherence-gate types (`CoherenceDecision`, `GateContext`, `RegimeLabel`) and the gate-evaluation logic. Uses `serde` for witness serialisation and `anyhow` for error reporting.

See `../CLAUDE.md` for context.
