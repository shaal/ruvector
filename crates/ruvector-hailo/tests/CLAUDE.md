# ruvector-hailo/tests

## Files

- `cpu_fallback_integration.rs` — end-to-end embedding pipeline via the CPU fallback path (feature `cpu-fallback`).
- `tokenizer_proptest.rs` — proptest invariants for `WordPieceTokenizer` (idempotence, special-id handling, round-trip).
