# ruvix-types/tests

Integration tests for the kernel interface types.

## Files

- `types_test.rs` — broad coverage of the public type surface (handles, capabilities, regions, queues).
- `proof_cache_test.rs` — focused on `proof_cache` / `proof_cache_optimized` correctness.
