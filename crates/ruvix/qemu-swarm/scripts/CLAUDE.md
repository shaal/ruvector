# ruvix-qemu-swarm/scripts

Shell helpers around the swarm orchestrator binary.

## Files

- `launch-swarm.sh` — convenience wrapper to spin up a swarm from one of the `configs/*.toml` files.
- `monitor.sh` — attach to / monitor a running swarm (serial consoles, status).
