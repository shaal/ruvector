# ruvix-qemu-swarm/configs

TOML cluster topologies consumed by `ruvix-qemu-swarm`.

## Files

- `single-node.toml` — minimal single-instance config.
- `3-node-cluster.toml` — 3-node cluster (basic quorum / replication tests).
- `8-node-swarm.toml` — 8-node swarm (scale-out tests).
