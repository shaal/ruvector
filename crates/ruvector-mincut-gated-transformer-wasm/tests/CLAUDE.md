# ruvector-mincut-gated-transformer-wasm/tests

- `web.rs` — `wasm-bindgen-test` integration test exercising the JS-facing API (`WasmTransformer`, `WasmGatePacket`, `infer`). Run with `wasm-pack test --headless --firefox` (or similar).

See `../CLAUDE.md`.
