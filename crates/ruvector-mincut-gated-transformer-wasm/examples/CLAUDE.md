# ruvector-mincut-gated-transformer-wasm/examples

- `web_scorer.rs` — runnable example showing the JS API: construct `WasmTransformer`, build a `WasmGatePacket`, run `infer(tokens, gate)`, and consume the witness (`decision`, `reason`, `logits`).

See `../CLAUDE.md`.
