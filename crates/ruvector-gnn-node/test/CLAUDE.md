# ruvector-gnn-node/test

Node test suite for the NAPI bindings.

## Files
- `basic.test.js` - smoke test that loads the addon and exercises core
  bindings. Run via the npm test runner declared in `package.json`.
