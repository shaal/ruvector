# ruvector-bench/scripts

Driver scripts for the benchmark suite.

## Files

- `download_datasets.sh` — pull ANN-Benchmarks datasets (SIFT, GIST, GloVe, etc.) used by `ann-benchmark`.
- `run_all_benchmarks.sh` — orchestrate all six benchmark binaries in sequence, collecting JSON results.
