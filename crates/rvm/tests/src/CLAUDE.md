# rvm/tests/src

- `lib.rs` — root of the cross-crate `rvm-tests` package; declares the integration test modules that exercise the full RVM stack (boot → capability → proof → witness → partition → scheduler → memory → coherence → wasm → security → kernel).

See `../CLAUDE.md`.
