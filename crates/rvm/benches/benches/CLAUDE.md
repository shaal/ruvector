# rvm/benches/benches

Criterion benchmark sources for the RVM workspace.

- `coherence.rs` — `[[bench]] name = "coherence"` — `rvm-coherence` scoring, mincut heuristic, adaptive recompute.
- `witness.rs` — `[[bench]] name = "witness"` — `rvm-witness` record emission, hash-chain extension, replay.
- `rvm_bench.rs` — `[[bench]] name = "rvm_bench"` — end-to-end / mixed-subsystem benchmarks.

See `../CLAUDE.md`.
