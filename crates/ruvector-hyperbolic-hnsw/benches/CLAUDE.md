# ruvector-hyperbolic-hnsw/benches

Criterion benchmarks.

## Files

- `hyperbolic_bench.rs` — Indexing throughput and query latency vs Euclidean HNSW baseline (HTML reports enabled).

Run via `cargo bench -p ruvector-hyperbolic-hnsw`.
