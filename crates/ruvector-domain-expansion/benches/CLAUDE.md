# ruvector-domain-expansion/benches

Criterion benchmark targets.

## Files

- `domain_expansion_bench.rs` — exercises `DomainExpansionEngine::new()`, `MetaThompsonEngine`, `PopulationSearch`, transfer-prior application, and cost-curve / pareto / plateau utilities. Registered as `[[bench]] name = "domain_expansion_bench"` in `../Cargo.toml`.
