# ruvector-core/examples

Runnable examples — `cargo run --example <name>`.

- `embeddings_example.rs` — demonstrates the embedding API (hash vs. ONNX).
- `neon_benchmark.rs` — ARM NEON SIMD micro-benchmark.
