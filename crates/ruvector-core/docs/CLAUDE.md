# ruvector-core/docs

User-facing markdown docs.

- `EMBEDDINGS.md` — guidance on embedding choice (hash placeholder vs. `OnnxEmbedding`), including the critical warning that the default `AgenticDB` embedder is character-bag and not semantic.
