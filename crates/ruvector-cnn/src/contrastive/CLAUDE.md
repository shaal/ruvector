# ruvector-cnn/src/contrastive

Contrastive-learning utilities for training CNN embeddings.

## Files

- `mod.rs` — module entry.
- `augmentation.rs` — image augmentation pipeline (behind `augmentation` feature).
- `infonce.rs` — InfoNCE loss.
- `triplet.rs` — triplet loss with hard-negative mining.
