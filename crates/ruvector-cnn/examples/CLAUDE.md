# ruvector-cnn/examples

## Files

- `graph_rewrite_demo.rs` — walks through the FP32 → INT8 graph rewrite pass and runs both versions to compare outputs.

Run via `cargo run --example graph_rewrite_demo -p ruvector-cnn`.
