# ruvector-sparse-inference-wasm/tests

WASM browser tests using `wasm-bindgen-test`.

## Files

- `web.rs` — Exercises `SparseInferenceEngine` in a headless browser (constructor + `infer`).

Run via `wasm-pack test --headless --chrome crates/ruvector-sparse-inference-wasm`.
