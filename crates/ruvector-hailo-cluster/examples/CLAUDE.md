# ruvector-hailo-cluster/examples

- `hailo-cluster-as-provider.rs` — demonstrates wrapping `HailoClusterEmbedder` as a `ruvector_core::EmbeddingProvider` (closes ADR-178 Gap B), the headline integration promised by ADR-167 §8.4.

See `../CLAUDE.md`.
