# ruvector-hailo-cluster/tests/common

Shared helpers for the integration tests in `../`.

- `mod.rs` — fixture helpers (worker spawning via the `fakeworker` bin, transport builders, fingerprint setup, TLS test certs, etc.).

See `../CLAUDE.md`.
