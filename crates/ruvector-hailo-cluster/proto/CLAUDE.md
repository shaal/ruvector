# ruvector-hailo-cluster/proto

- `embedding.proto` — gRPC service contract between `ruvector-hailo-embed` / coordinator and `ruvector-hailo-worker`. Compiled by `../build.rs` via `tonic-build`.

See `../CLAUDE.md`.
