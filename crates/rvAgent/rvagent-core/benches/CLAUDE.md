# rvagent-core/benches

Criterion benchmarks for core hot paths.

- `state_bench.rs` — `AgentState` clone / mutation throughput (Arc-based).
- `rvf_bridge_bench.rs` — `rvf_bridge` manifest/witness operations.
