# rvagent-core/examples

- `agi_container_demo.rs` — build an AGI container from RVF segments using
  `AgiContainerBuilder` (B1).
- `cow_state_demo.rs` — demonstrate the copy-on-write state backend (B2).
- `session_crypto_demo.rs` — encrypt/decrypt a session with `SessionCrypto`.
