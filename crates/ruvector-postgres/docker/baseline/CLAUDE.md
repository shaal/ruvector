# ruvector-postgres/docker/baseline

Placeholder directory for the baseline (pgvector reference) container assets used during comparative benchmarks. Currently empty — populate when introducing baseline runs.
