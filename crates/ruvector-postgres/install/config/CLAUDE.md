# ruvector-postgres/install/config

Configuration templates emitted by the installer.

## Files

- `ruvector.conf.template` — Template `postgresql.conf` fragment with recommended GUCs for the extension (shared preload libraries, memory settings, worker counts).
