# ruvector-postgres/benches/sql

SQL-level benchmark workloads run against a live PostgreSQL instance with the extension installed.

## Files

- `benchmark_workload.sql` — Full workload (inserts, indexes, queries) measuring sustained throughput.
- `quick_benchmark.sql` — Smaller smoke benchmark for fast feedback.
