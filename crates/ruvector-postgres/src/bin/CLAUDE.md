# ruvector-postgres/src/bin

pgrx infrastructure binaries.

## Files

- `pgrx_embed.rs` — Embedded build helper required by pgrx for the cdylib build/test workflow. Do not invoke manually; it is consumed by `cargo pgrx`.
