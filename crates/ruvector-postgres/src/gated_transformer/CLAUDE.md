# ruvector-postgres/src/gated_transformer

Mincut-gated transformer integration — feature-gated under `gated-transformer`.

## Files

- `mod.rs` — Sole module file; wraps `ruvector-mincut-gated-transformer` and exposes SQL functions.

## Pointers

- Backbone crate: `ruvector-mincut-gated-transformer` (not in this chunk).
- Related: `../../../ruQu/` (uses the same gating principle from the quantum side).
