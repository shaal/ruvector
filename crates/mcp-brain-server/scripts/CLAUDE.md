# mcp-brain-server/scripts

Operational shell scripts for the brain server.

## Files

- `pubmed_discover.sh` — runs PubMed ingest discovery (paired with `src/pubmed.rs`).

Add other operational scripts here. For deploys see `cloud/` and root-level `deploy.sh`.
