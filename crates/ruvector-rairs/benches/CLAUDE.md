# ruvector-rairs/benches

## Files

- `rairs_bench.rs` — criterion bench comparing `IvfFlat`, `RairsStrict`, `RairsSeil` recall/throughput at various `nprobe` settings.

Run via `cargo bench -p ruvector-rairs`. The `rairs-demo` binary in `src/main.rs` is the de-facto larger-scale benchmark.
