# ruvector-attention-cli/config

Shipped configuration files for the `ruvector-attention` CLI.

## Files
- `default.toml` - default attention engine + server configuration applied
  when the user does not pass `--config`. Loaded by `src/config.rs`.
