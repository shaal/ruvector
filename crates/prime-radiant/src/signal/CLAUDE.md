# prime-radiant/src/signal

Input signal pipeline before the coherence engine sees it.

## Files

- `mod.rs` — module entry.
- `ingestion.rs` — accepts raw input events into the substrate.
- `normalization.rs` — normalizes inputs (scaling, encoding).
- `validation.rs` — domain-specific validation pass.
