# ruvector-verified-wasm/tests

- `web.rs` — `wasm-bindgen-test` integration test running in headless browsers. Exercises `JsProofEnv` (proof creation, batch verification, attestations, stats).

Run with `wasm-pack test --headless --firefox` (or chrome). See `../CLAUDE.md`.
