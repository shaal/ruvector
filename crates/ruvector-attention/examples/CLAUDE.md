# ruvector-attention/examples

## Files

- `hyperbolic_bench.rs` — example demonstrating the hyperbolic attention path (despite the name, it is shipped as an example, not a bench).

Run with `cargo run --example hyperbolic_bench -p ruvector-attention`.
