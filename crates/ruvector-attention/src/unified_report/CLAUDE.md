# ruvector-attention/src/unified_report

Unified reporting across the many attention mechanisms — collect metrics and emit a single report.

## Files

- `mod.rs` — module entry.
- `metrics.rs` — metric value objects.
- `report.rs` — `Report` type + builder / formatter.
