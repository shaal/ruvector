# ruvector-decompiler/examples

- `run_on_cli.rs` — example binary that decompiles a JS bundle from a CLI argument
  and prints the resulting modules. Run with `cargo run --example run_on_cli --
  <bundle.js>`.
