# ruQu/docs/adr

Architecture Decision Records (ADRs) for `ruqu`.

## Files

- `ADR-001-ruqu-architecture.md` — Top-level architecture: two-layer classical control (RuVector memory layer + dynamic min-cut gate), 256-tile WASM fabric, DDD bounded contexts.

Add new ADRs as `ADR-NNN-<short-title>.md` and link them from the crate-level `lib.rs` doc.
