# ruvector-coherence/tests

## Files

- `spectral_bench.rs` — exercises the spectral path (Fiedler / spectral gap / largest eigenvalue / effective resistance). Named `_bench` but runs as an integration test under `cargo test --features spectral`.
