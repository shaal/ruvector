# ruvllm/models

Shipped model specifications. These are consumed by code in
`src/models/` and the hub registry in `src/hub/registry.rs`.

## Files
- `ruvltra_small.json` - RuvLTRA-Small (Qwen 0.5B) architecture +
  default-quantization spec.
