# ruvllm/src/lora/adapters

Adapter trainer + merge utilities used by the LoRA pipeline.

## Files
- `mod.rs` - sub-module wiring.
- `trainer.rs` - LoRA / MicroLoRA training step implementations.
- `merge.rs` - merge an adapter into base weights (or stack adapters).
