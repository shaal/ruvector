# ruvector-router-cli/src

Sole source dir.

## Files

- `main.rs` — clap `Cli`/`Commands` enum driving `ruvector_router_core::VectorDB` operations (create, insert, search, distance metric selection, benchmarking). Uses `colored` for pretty output and `tracing-subscriber` for logging.
