# rvf/tests

Workspace-wide test crates for the RVF family.

## Subdirs

- `rvf-integration/` — `rvf-integration-tests` crate (publish = false). Integration and acceptance tests covering wire round-trip, manifest boot, HNSW recall, quantization accuracy, kernel selection, lineage/attestation, COW branching/recovery, crash safety, CLI smoke, file identity, cross-platform compatibility, and more.
