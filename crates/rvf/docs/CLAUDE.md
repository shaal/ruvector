# rvf/docs

Design records and security audits for the RVF workspace.

## Subdirs

- `adr/` — Architecture Decision Records (ADRs).
- `security/` — security audits.

Note: most ADRs for the wider monorepo live under `docs/research/...` at the repo root; this dir holds RVF-specific records.
