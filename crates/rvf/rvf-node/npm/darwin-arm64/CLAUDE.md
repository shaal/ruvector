# rvf-node/npm/darwin-arm64

Prebuilt N-API binary for macOS aarch64 (Apple Silicon). Contains the `.node` artifact and a sub-package.json published as an optional dependency of `rvf-node`. Regenerated by `napi build --target aarch64-apple-darwin`.
