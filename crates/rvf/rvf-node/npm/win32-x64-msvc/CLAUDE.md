# rvf-node/npm/win32-x64-msvc

Prebuilt N-API binary for Windows x86_64 (MSVC). Contains the `.node` artifact and a sub-package.json published as an optional dependency of `rvf-node`. Regenerated by `napi build --target x86_64-pc-windows-msvc`.
