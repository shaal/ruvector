# rvf-node/npm/darwin-x64

Prebuilt N-API binary for macOS x86_64 (Intel). Contains the `.node` artifact and a sub-package.json published as an optional dependency of `rvf-node`. Regenerated by `napi build --target x86_64-apple-darwin`.
