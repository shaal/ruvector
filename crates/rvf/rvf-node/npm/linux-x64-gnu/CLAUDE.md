# rvf-node/npm/linux-x64-gnu

Prebuilt N-API binary for Linux x86_64 (glibc). Contains the `.node` artifact and a sub-package.json published as an optional dependency of `rvf-node`. Regenerated by `napi build --target x86_64-unknown-linux-gnu`.
