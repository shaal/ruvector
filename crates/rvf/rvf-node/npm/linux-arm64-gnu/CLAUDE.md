# rvf-node/npm/linux-arm64-gnu

Prebuilt N-API binary for Linux aarch64 (glibc). Contains the `.node` artifact and a sub-package.json published as an optional dependency of `rvf-node`. Regenerated by `napi build --target aarch64-unknown-linux-gnu`.
