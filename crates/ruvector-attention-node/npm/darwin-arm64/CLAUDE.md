# @ruvector/attention-darwin-arm64

Optional dependency package for macOS arm64.

- `package.json` — `os: [darwin]`, `cpu: [arm64]`, `main: attention.darwin-arm64.node`.
- `attention.darwin-arm64.node` — prebuilt NAPI binary.

Selected at install time when running on Apple Silicon macOS.
