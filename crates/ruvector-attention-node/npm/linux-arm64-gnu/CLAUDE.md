# @ruvector/attention-linux-arm64-gnu

Optional dependency package for Linux aarch64 (glibc).

- `package.json` — `os: [linux]`, `cpu: [arm64]`, libc `glibc`.
- `attention.linux-arm64-gnu.node` — prebuilt NAPI binary.
