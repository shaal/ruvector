# @ruvector/attention-win32-arm64-msvc

Optional dependency package for Windows ARM64 (MSVC).

- `package.json` — `os: [win32]`, `cpu: [arm64]`.
- `.node` artefact populated by `napi build` / `napi artifacts` during release.
