# @ruvector/attention-linux-x64-musl

Optional dependency package for Linux x86_64 (musl libc, e.g. Alpine).

- `package.json` — `os: [linux]`, `cpu: [x64]`, libc `musl`.
- `.node` artefact populated by `napi build` / `napi artifacts` during release.
