# @ruvector/attention-linux-x64-gnu

Optional dependency package for Linux x86_64 (glibc).

- `package.json` — `os: [linux]`, `cpu: [x64]`, libc `glibc`.
- `attention.linux-x64-gnu.node` — prebuilt NAPI binary.
