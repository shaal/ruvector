# @ruvector/attention-darwin-x64

Optional dependency package for macOS x86_64.

- `package.json` — `os: [darwin]`, `cpu: [x64]`, `main: attention.darwin-x64.node`.
- `attention.darwin-x64.node` — prebuilt NAPI binary.
