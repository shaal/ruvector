# ruvector-mincut/src/monitoring

Event-driven monitoring API: callers register thresholds and receive notifications when the cut value crosses them.

- `mod.rs` — `Monitor`, event types, threshold registration.
