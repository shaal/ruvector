# ruvector-mincut/src/connectivity

Dynamic graph connectivity primitives used as subroutines by higher-level cut algorithms.

- `mod.rs` — connectivity façade.
- `polylog.rs` — polylogarithmic-time connectivity (Holm-Lichtenberg-Thorup-style).
- `cache_opt.rs` — cache-optimised connectivity variant.
