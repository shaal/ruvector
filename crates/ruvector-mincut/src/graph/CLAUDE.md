# ruvector-mincut/src/graph

Internal graph representation used by the min-cut algorithms (distinct from `petgraph` — tuned for cut maintenance).

- `mod.rs` — adjacency / edge-id layout, mutation API, iteration helpers.
