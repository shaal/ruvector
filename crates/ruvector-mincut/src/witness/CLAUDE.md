# ruvector-mincut/src/witness

Witness chain for min-cut decisions: hashed records that let an outside observer verify a sequence of cut updates without re-running them.

- `mod.rs` — `Witness`, witness-chain assembly, verification API.

Pairs with `certificate/` (proof certificates). See `docs/witness-quick-reference.md`, `docs/witness-trees.md`.
