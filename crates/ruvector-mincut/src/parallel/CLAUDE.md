# ruvector-mincut/src/parallel

Parallel-execution primitives (separate from `optimization/parallel.rs` which holds parallel optimization variants).

- `mod.rs` — parallel scheduling and worker management primitives.
