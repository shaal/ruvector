# ruvector-mincut/src/fragment

Fragment-graph abstraction used during dynamic updates (small pieces that can be repaired independently).

- `mod.rs` — fragment data structure and merge / split helpers.

See also `fragmentation/` which drives when and how the graph is split into fragments.
