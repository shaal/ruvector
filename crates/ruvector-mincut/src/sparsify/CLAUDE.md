# ruvector-mincut/src/sparsify

Cut-preserving graph sparsification (Benczúr-Karger style). Used by the (1+ε)-approximate algorithm and inside the J-tree.

- `mod.rs` — sparsifier construction and incremental update.

See also `docs/sparsification_implementation.md` and `examples/sparsify_demo.rs`.
