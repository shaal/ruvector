# ruvector-mincut/src/wrapper

Outward-facing wrapper API: a high-level handle that bundles the dynamic min-cut with connectivity-curve analysis and friendly defaults. Exposed via WASM as `WasmMinCutWrapper`. Tested in `tests/wrapper_tests.rs`.

- `mod.rs` — wrapper façade and ergonomic builder.
