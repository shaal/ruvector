# ruvector-mincut/src/integration

External-system integration façades (kept separate from `wrapper/` which exposes the broader user API).

- `mod.rs` — integration points used by downstream RuVector crates.
