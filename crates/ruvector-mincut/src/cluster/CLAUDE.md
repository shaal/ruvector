# ruvector-mincut/src/cluster

Cluster decomposition used by the 3-level hierarchy (Expander → Precluster → Cluster).

- `mod.rs` — `Cluster` type and decomposition API.
- `hierarchy.rs` — hierarchical clustering driver used by the J-tree (`src/jtree/`).
