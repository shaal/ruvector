# ruvector-attention-wasm/js

TypeScript wrapper that surfaces the WASM bindings with typed JS APIs.

## Files

- `index.ts` — Main entry; re-exports and high-level helpers.
- `types.ts` — TypeScript definitions matching `pkg/ruvector_attention_wasm.d.ts`.

Compiled via the `tsconfig.json` at the crate root.
