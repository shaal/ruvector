# ruvllm-wasm/tests

## Files

- `web.rs` — wasm-bindgen-test driven; run via `wasm-pack test --node` or similar.
- `intelligent_wasm_test.rs` — end-to-end `IntelligentLLMWasm` coverage (routing + LoRA + SONA).
