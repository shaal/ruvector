# ruvector-sparse-inference/src/integration

Glue layers wiring sparse inference into other RuVector subsystems.

- `mod.rs` — module roots.
- `ruvector.rs` — bridge into the main `ruvector` engine.
- `ruvllm.rs` — bridge into the `ruvllm` LLM-serving stack.
