# ruvector-sparse-inference/tests/unit

Unit tests per subsystem.

- `predictor_tests.rs` — low-rank predictor (`src/predictor/lowrank.rs`).
- `quantization_tests.rs` — precision lanes and quantizers (`src/precision/`).
- `sparse_ffn_tests.rs` — sparse FFN op (`src/sparse/ffn.rs`).
