# ruvector-sparse-inference/tests/property

Proptest-based property tests for inference invariants.

- `mod.rs` — property strategies and `proptest!` blocks (e.g. quantize/dequantize
  round-trip bounds, predictor monotonicity).
