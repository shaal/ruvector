# ruvector-verified/benches

Criterion benchmarks.

- `arena_throughput.rs` — `fast-arena` allocation throughput (feature `fast-arena`).
- `proof_generation.rs` — end-to-end proof-generation latency; validates the sub-microsecond overhead claim.
