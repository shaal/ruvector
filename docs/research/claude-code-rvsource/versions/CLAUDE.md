# docs/research/claude-code-rvsource/versions/

Versioned snapshots of claude-code extractions.

## Subdirs

- `v2.1.x/` - claude-code v2.1.x extraction with `tree/` per-feature directories, `modules-manifest.json`, `witness.json`, and `metrics.json`.

## Related

- `../extracted/` - flat per-domain view of the same artifacts.
