# docs/research/claude-code-rvsource/versions/v2.1.x/

Versioned snapshot for claude-code **v2.1.x**.

## Files

- `modules-manifest.json` - full module manifest.
- `metrics.json` - extraction metrics.
- `witness.json` - witness / attestation.

## Subdirs

- `tree/` - per-feature subdirectories named after a representative symbol from each module cluster (e.g. `asyncgenerator/`, `bedrockclient/`, `react_memo_cache_sentinel/`, `select-pane/`). Each contains the JS files clustered together.

## Related

- `../../extracted/` - flat per-domain extraction.
- `../../00-index.md` - top-level prose index.
