# tree/asyncgenerator/

Machine-extracted JS module cluster from claude-code v2.1.x. Files were grouped around references to async-generator constructs, with many incidental utility shims (Cache.add/put, FormData, telemetry env vars, headers, etc.) pulled in.

Notable files include `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC.js`, `MEMORY_md.js`, `OTEL_EXPORTER_OTLP_ENDPOINT.js`, `Cache_add.js`, `Cache_put.js`, `FormData_delete.js`, `correlationId.js`, `headers.js`, `latin1.js`, plus many `_*.js` private helpers.

## Related

- `../` - sibling clusters and the cluster overview.
- `../../../extracted/source/core/` - same code re-organized by domain.
