# docs/sparse-inference/

Docs for the sparse-inference subsystem. Currently a single architecture doc; most detailed work lives in ADRs.

## Docs

- `ARCHITECTURE.md` - sparse inference architecture.

## Related

- `../adr/` ADR-183 .. ADR-192 - sparse attention hardening ADR run (rand dependency, online softmax, landmark fix, KV cache, GQA/MQA, etc.).
- `../research/quantization-edge/09-triattention-kv-sparsity.md` - related sparsity research.
