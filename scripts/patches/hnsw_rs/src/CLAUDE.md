Mirror of `../../../../patches/hnsw_rs/src/`. See that directory's CLAUDE.md for file-by-file descriptions.

Files: `api.rs`, `datamap.rs`, `filter.rs`, `flatten.rs`, `hnsw.rs` (core graph), `hnswio.rs` (serialization/mmap IO), `lib.rs`, `libext.rs` (C FFI), `prelude.rs`.
