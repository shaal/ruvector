# ui/ruvocal/stub/@reflink/

npm scope directory holding the local `@reflink/reflink` stub package.

## Subdirectories

- `reflink/` — empty stub of `@reflink/reflink` resolved by `package.json` `overrides`.
