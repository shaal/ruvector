# ui/ruvocal/src/routes/models/

Public model catalog page (`/models`).

## Files

- `+page.svelte` — lists available models with cards (uses `lib/components/ModelCardMetadata.svelte`).

## Subdirectories

- `[...model]/` — detail page for a specific model (rest-segment lets the URL preserve `namespace/model`).
