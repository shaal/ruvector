# ui/ruvocal/src/routes/api/v2/user/billing-orgs/

Lists the billing organizations the authenticated user is part of.

## Files

- `+server.ts` — `GET` returns the user's billing orgs from the upstream HF account (used by Pro / quota UI).
