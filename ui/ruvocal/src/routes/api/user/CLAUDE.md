# ui/ruvocal/src/routes/api/user/

Legacy current-user endpoint.

## Files

- `+server.ts` — `GET` returns the authenticated user profile / settings.

## Subdirectories

- `validate-token/` — validate an API token without performing an action.
