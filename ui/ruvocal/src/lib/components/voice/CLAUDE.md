# ui/ruvocal/src/lib/components/voice/

Voice-input UI components.

## Files

- `AudioWaveform.svelte` — live waveform visualization rendered while the user is recording via `chat/VoiceRecorder.svelte`. The captured audio is sent to `/api/transcribe`.
