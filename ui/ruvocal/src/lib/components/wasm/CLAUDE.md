# ui/ruvocal/src/lib/components/wasm/

UI components that surface the in-browser WASM (`rvagent_wasm`) capabilities.

## Files

- `GalleryPanel.svelte` — gallery panel showing WASM-tool outputs / artifacts.

## Related

- WASM loader/IDB: `../../wasm/`.
- WASM artifacts: `static/wasm/rvagent_wasm.js` and `rvagent_wasm_bg.wasm`.
- Store: `../../stores/wasmMcp.ts`.
