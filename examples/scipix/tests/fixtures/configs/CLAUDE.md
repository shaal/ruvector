# scipix/tests/fixtures/configs

TOML config fixtures for tests.

## Files

- `test_config.toml` - Minimal config used by config/error/CLI tests.

## Related

- Used by: `../../unit/config_tests.rs`, `../../integration/cli_tests.rs`.
- Production config types: `../../../src/config.rs`.
