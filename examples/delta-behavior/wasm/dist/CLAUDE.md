# delta-behavior / wasm / dist

Build output of `tsup` for the `@ruvector/delta-behavior` package. Generated; do not edit by hand.

## Important files
- `index.cjs`, `index.cjs.map`, `index.d.cts` - CommonJS bundle + sourcemap + type declarations.
- `types.cjs`, `types.cjs.map`, `types.d.cts`, `types.js`, `types.js.map`, `types.d.ts` - shared TypeScript types entrypoint built from `../src/types.ts`.

## Regenerate
- `npm run build` (or `npm run build:all` to include the WASM package).

## Related
- Source: `../src/`. Bundler config: `../tsup.config.ts`. Package manifest: `../package.json`.
