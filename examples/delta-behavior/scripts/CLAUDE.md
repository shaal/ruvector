# delta-behavior / scripts

Helper shell scripts for the delta-behavior crate.

## Important files
- `build-wasm.sh` - convenience wrapper around `wasm-pack` for building the WASM module consumed by `../wasm/`.

## Related
- WASM package: `../wasm/`. Architecture: `../research/WASM-DELTA-ARCHITECTURE.md`.
