# edge / scripts

Helper scripts for the edge crate.

## Important files
- `build-wasm.sh` - convenience wrapper around `wasm-pack` that builds the WASM module into `../pkg/`.

## Related
- WASM output + demos: `../pkg/`. Cargo WASM feature: `wasm` in `../Cargo.toml`.
