# OSpipe / src / quantum

Experimental quantum-inspired primitives used by OSpipe (e.g. collapse/coherence-style operators borrowed from the `cognitum-gate-kernel` and `ruqu-algorithms` workspace crates).

## Important files
- `mod.rs` - module root and only file; defines the quantum-inspired API surface consumed elsewhere.

## Related
- Workspace crates: `cognitum-gate-kernel`, `ruqu-algorithms` (pulled in via `../../Cargo.toml`).
- Sibling research-oriented modules: `../../../delta-behavior/`, `../../../cmb-consciousness/`.
