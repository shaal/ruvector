# mincut/benchmarks

`benchmarks` example for the mincut crate — runs perf measurements over the exact/approximate MinCut paths.

## Important files
- `main.rs` — benchmark entry; runs configurations and prints timings.

## Run
- From `../`: `cargo run --release --example benchmarks`.
