# mincut/neural_optimizer

Neural-optimizer demo: uses MinCut signals as part of a neural-net optimization loop.

## Important files
- `main.rs` — optimizer driver.

## Run
- From `../`: `cargo run --release --example neural_optimizer`.
