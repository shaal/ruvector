# mincut/federated_loops

Federated-loop demo: multiple agents iteratively cut/merge subgraphs and exchange boundary info.

## Important files
- `main.rs` — multi-agent loop driver.

## Run
- From `../`: `cargo run --release --example federated_loops`.
