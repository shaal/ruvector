# mincut/temporal_hypergraph

Demonstrates MinCut over temporal hypergraphs (edges connect >2 vertices and evolve over time).

## Important files
- `main.rs` — temporal hypergraph + cut analysis driver.

## Run
- From `../`: `cargo run --release --example temporal_hypergraph`.
