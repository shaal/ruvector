# mincut/temporal_attractors/src

Source for the standalone `temporal-attractors` binary.

## Important files
- `main.rs` — builds a temporal attractor network and reports MinCut-derived convergence behaviour.

## Build
- From parent: `cargo run --release`.
