# robotics/src

Source for the robotics tutorial crate.

## Files

- `main.rs` - Default binary (entry-point/landing text).
- `bin/` - Numbered tutorial binaries (`01_basic_perception` ... `10_full_pipeline`).

## Related

- Per-binary list and details: `./bin/CLAUDE.md`.
- Underlying crate: `crates/ruvector-robotics`.
