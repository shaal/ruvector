# seti-boundary-discovery/src

Source for the SETI boundary-discovery demo.

## Important files
- `main.rs` — builds a synthetic SETI signal graph, runs exact MinCut + coherence, prints boundary candidates.

## Build
- From parent: `cargo run --release`.
