# learning-scenarios/diverse-patterns

Assets for the diverse-patterns learning scenario.

## Important files
- `config.yaml` — scenario configuration.
- `patterns.json` — labeled pattern dataset.
- `setup.sh` — bootstrap script.
- `types.ts` — TypeScript types shared with the JS-side tooling.

## Related
- Scenario consumer: `../attention_patterns.rs` and `../mod.rs`.
