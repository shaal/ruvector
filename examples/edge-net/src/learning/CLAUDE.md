# edge-net/src/learning

Online-learning subsystem (small module; concrete scenarios live in `../learning-scenarios/`).

## Important files
- `mod.rs` — learning loop entry point; pairs with the `learning-enhanced` feature flag.

## Related
- Scenarios: `../learning-scenarios/`. Tests: `../../tests/learning_scenarios_test.rs`.
