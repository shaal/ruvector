# edge-net/src/capabilities

Capability advertisement: each contributor declares what it can do (compute backends, model support, bandwidth).

## Important files
- `mod.rs` — capability type, serialization, helpers.
