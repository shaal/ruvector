# edge-net/src/ai/sona

Sona reasoning bank: trajectory-aware adaptive learning with LoRA-based memory.

## Important files
- `mod.rs` — module entry.
- `types.rs` — shared types.
- `lora.rs` — LoRA adapter for Sona.
- `reasoning_bank.rs` — pattern recognition / verdict judgement memory.
- `trajectory.rs` — trajectory tracking.

## Related
- See the `reasoningbank-agentdb` and `reasoningbank-intelligence` skills for the conceptual model.
