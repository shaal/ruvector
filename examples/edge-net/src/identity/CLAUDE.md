# edge-net/src/identity

Cryptographic identity primitives (ed25519 / x25519 / argon2-derived keys).

## Important files
- `mod.rs` — module entry; identity types and key management.

## Related
- See also `../pikey/` for the PiKey scheme.
- UI: `../../dashboard/src/components/identity/IdentityPanel.tsx`.
