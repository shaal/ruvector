# edge-net/src/network

P2P networking layer.

## Important files
- `mod.rs` — module entry.
- `p2p.rs` — peer connection management.
- `protocols.rs` — message/protocol definitions.
- `semantic.rs` — semantic routing (vector-similarity-based peer selection).

## Related
- JS-side: `../../pkg/{p2p,network,networks,dht,signaling}.js`.
