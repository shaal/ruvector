# edge-net/src/brain

Brain orchestrator: aggregates AI subsystems into a single "brain" surface exported to JS.

## Important files
- `mod.rs` — module entry; defines the Brain type and its WASM bindings.

## Related
- UI panel: `../../dashboard/src/components/brain/BrainStatus.tsx`.
