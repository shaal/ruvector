# edge-net/relay/tests

Tests for the WebSocket relay.

## Important files
- `relay.test.js` — uses Node's built-in `node:test` runner to exercise the relay.

## Run
- `npm test` from `../` (runs `node --test tests/relay.test.js`).
