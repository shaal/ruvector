# components/network

Network topology, stats, and visualization.

## Important files
- `NetworkStats.tsx` — TFLOPS / node counts / latency tiles.
- `NetworkVisualization.tsx` — graph-style network view (Recharts-based).
- `SpecializedNetworks.tsx` — view of specialized sub-networks.
