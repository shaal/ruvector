# dashboard/src/services

Backend clients used by stores and components.

## Important files
- `edgeNet.ts` — high-level edge-net WASM/HTTP client.
- `relayClient.ts` — WebSocket relay client (talks to `../../../relay/`).
- `storage.ts` — browser storage (localStorage/IndexedDB) wrapper.
