# edge-net/dashboard/public

Static assets served by Vite at the site root. Files here are copied verbatim into the build output.

## Important files
- `crystal.svg` — Time Crystal brand mark used in the UI.
- `vite.svg` — default Vite favicon.
