# dashboard/tests

Out-of-tree dashboard test material (CLI/manual tests, not the Vitest suite under `src/tests/`).

## Important files
- `CLI_TEST_REPORT.md` — recorded test report.
- `edge-net-cli-test.js` — Node script exercising the `edge-net` CLI from `../../pkg/`.
