# edge-net/sim/scripts

Post-simulation analysis helpers.

## Important files
- `generate-report.js` — turns raw simulator output into a structured report.
- `visualize.js` — produces visualizations from simulator output.

## Run
- After `npm run simulate`, pipe its output (or pass result paths) to one of these scripts.
