# edge-net/sim/dist

Compiled output of `npm run build` (TypeScript -> ESM JS + sourcemaps + `.d.ts`). Generated — do not edit.

## Important files
- `cell.{js,d.ts,*.map}`, `metrics.*`, `network.*`, `phases.*`, `report.*`, `simulator.*` — one per source file in `../src/`.

## Notes
- Regenerate via `npm run build` from `../`. Clean with `npm run clean`.
