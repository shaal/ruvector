# edge-net/sim/examples

Small standalone usage demos for the lifecycle simulator.

## Important files
- `quick-demo.js` — minimal example that imports the built simulator and runs a short scenario.

## Run
- `npm run build` then `node examples/quick-demo.js`.
