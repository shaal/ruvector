# edge-net/scripts

Convenience scripts for the edge-net crate.

## Important files
- `run-benchmarks.sh` — builds release and runs the bench harness in `../benches/`.

## Notes
- A top-level `../run-benchmarks.sh` shim exists for the same purpose.
