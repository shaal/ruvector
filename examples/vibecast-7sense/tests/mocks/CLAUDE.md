# vibecast-7sense/tests/mocks

Mock implementations of repository traits used by the integration tests.

## Files
- `mod.rs` - Defines mock repositories (in-memory stand-ins for the production adapters in each `crates/sevensense-*/src/infrastructure/`).

## Related
- Used by: `../integration/` test files.
- Fixtures: `../fixtures/`.
