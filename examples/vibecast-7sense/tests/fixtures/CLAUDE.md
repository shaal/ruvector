# vibecast-7sense/tests/fixtures

Shared deterministic test fixtures for the workspace integration tests.

## Files
- `mod.rs` - Aggregates fixture helpers (sample audio metadata, canned embeddings, deterministic seeds).

## Related
- Used by: `../integration/` test files.
- Mock implementations: `../mocks/`.
