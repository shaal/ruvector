# sevensense-embedding/src/domain

Domain layer of the embedding bounded context.

## Files
- `mod.rs` - Aggregates and re-exports.
- `entities.rs` - `Embedding` (1536-dim vector + metadata) and `EmbeddingModel` (model descriptor).
- `repository.rs` - Repository traits implemented by infrastructure.

## Related
- Application layer: `../application/`.
- Infrastructure: `../infrastructure/`.
