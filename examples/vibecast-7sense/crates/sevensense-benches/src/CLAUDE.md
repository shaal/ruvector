# sevensense-benches/src

Library source for shared benchmark utilities.

## Files
- `lib.rs` - Crate root; re-exports helpers.
- `utils.rs` - Synthetic data generation, timers, and other helpers reused across the criterion benches.

## Related
- Benchmark targets: `../benches/`.
- Wrapper script: `../../../scripts/run_benchmarks.sh`.
