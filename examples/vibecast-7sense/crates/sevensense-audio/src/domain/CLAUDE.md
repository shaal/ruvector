# sevensense-audio/src/domain

Domain layer of the audio bounded context.

## Files
- `mod.rs` - Re-exports.
- `entities.rs` - `Recording`, `CallSegment` entities and their invariants.
- `repository.rs` - Repository traits implemented by `../infrastructure/`.

## Related
- Application layer: `../application/`.
- Infrastructure: `../infrastructure/`.
