# benchmarks/tests

Integration tests for the `ruvector-benchmarks` crate.

## Important files
- `integration_tests.rs` — exercises the benchmark binaries/library code end-to-end (small-scale variants to keep CI fast).

## Run
- `cargo test` from `../` (the crate root).
