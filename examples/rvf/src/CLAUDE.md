# rvf/src

Minimal shared library for the `rvf-examples` crate.

## Files

- `lib.rs` - Tiny helpers shared across the `examples/` binaries (the heavy lifting lives in the underlying `rvf-*` crates and in each example file).

## Related

- Examples: `../examples/`.
- RVF crate suite: `crates/rvf/`.
