# ruvLLM / esp32-flash / src / models

Shipped model catalog / loaders for the flashable firmware.

## Important files
- `mod.rs` - module root; lists supported model variants and exposes the load helpers consumed by `../main.rs` and the inference modules.

## Related
- Larger sibling catalog: `../../../esp32/src/models/`. Optimizations: `../optimizations/`.
