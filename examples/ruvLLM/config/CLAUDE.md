# ruvLLM / config

Sample configuration files for ruvLLM.

## Important files
- `example.toml` - reference TOML config consumed by `../src/config.rs` (model paths, storage backend, server, SONA, learning loops, etc.).

## Related
- Loader: `../src/config.rs`. Server: `../src/bin/server.rs`.
