# refrag-pipeline/benches

Criterion microbenchmarks for the REFRAG pipeline.

## Important files
- `refrag_bench.rs` — measures compress / sense / expand throughput against the in-memory store.

## Run
- `cargo bench` from `../`. Reports land in `target/criterion/`.
