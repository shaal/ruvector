# decompiler-dashboard/src/types

Shared TypeScript type declarations for the dashboard.

## Files

- `index.ts` — exports `VersionData`, `PageId`, and other type aliases
  consumed throughout `../components/`, `../lib/`, and `../pages/`.

## Related

- `../App.tsx` imports `VersionData` and `PageId` directly from here.
