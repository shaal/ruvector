# infrastructure-boundary-discovery/src

Source for the boundary-discovery demo.

## Important files
- `main.rs` — generates a synthetic infrastructure graph, runs exact MinCut + spectral coherence, prints findings.

## Build
- From parent: `cargo run --release`.
