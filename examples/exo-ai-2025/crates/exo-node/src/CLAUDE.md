# exo-node/src

## Files

- `lib.rs` — `#[napi]`-annotated functions/classes exposing the
  substrate to Node.js.
- `types.rs` — JS-friendly wrapper types (conversion to/from
  exo-core types).

## Related

- `../Cargo.toml` — NAPI-RS configuration / `cdylib` declaration
- `../../exo-backend-classical/` — backend driven from JS
