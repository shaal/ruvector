# exo-exotic/src/experiments

Inline experiment modules used by the exo-exotic crate, paralleling the
standalone Cargo workspaces under `../../../../research/`.

## Files

- `mod.rs` — module gate / re-exports.
- `causal_emergence.rs` — see `../../../../research/07-causal-emergence/`.
- `memory_mapped_fields.rs` — see
  `../../../../research/05-memory-mapped-neural-fields/`.
- `neuromorphic_spiking.rs` — see
  `../../../../research/01-neuromorphic-spiking/`.
- `quantum_superposition.rs` — see
  `../../../../research/02-quantum-superposition/`.
- `sparse_homology.rs` — see
  `../../../../research/04-sparse-persistent-homology/`.
- `time_crystal_cognition.rs` — see
  `../../../../research/03-time-crystal-cognition/`.

These versions are slimmed-down adaptations that fit the exo-exotic
trait set; the standalone research crates retain the full prototype.

## Related

- `../../../research/` — full standalone versions
