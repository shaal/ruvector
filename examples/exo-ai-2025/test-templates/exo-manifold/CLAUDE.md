# test-templates/exo-manifold

TDD scaffolding for the `exo-manifold` crate. Snapshot of the initial
test design — live version at `../../crates/exo-manifold/tests/`.

## Subdirectories

- `tests/` — contains `manifold_engine_test.rs`.

## Related

- `../../crates/exo-manifold/tests/`
