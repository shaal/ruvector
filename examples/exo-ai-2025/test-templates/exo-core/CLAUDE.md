# test-templates/exo-core

TDD scaffolding for the `exo-core` crate. Snapshot of the initial test
design — the live version lives at `../../crates/exo-core/tests/`.

## Subdirectories

- `tests/` — contains `core_traits_test.rs`.

## Related

- `../../crates/exo-core/tests/` — live tests
